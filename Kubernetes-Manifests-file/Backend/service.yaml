apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: todo
spec:
  ports:
    - port: 3500
      targetPort: 3500
      nodePort: 30500
      protocol: TCP
  type: NodePort
  selector:
    role: api
